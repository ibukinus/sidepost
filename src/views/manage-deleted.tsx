/**
 * 削除完了画面（screens.md 3.6）。
 * - 削除完了の旨と、削除済みデータの限界（既取得データ等は消えないこと。要件6.8）を
 *   固定文言で示す。削除理由（本当に削除したのか、すでに削除済みだったのか等）は区別しない。
 */
export function ManageDeleted() {
  return (
    <section class="manage-deleted">
      <h1>削除が完了しました</h1>
      <p>ネタバレ本文レコードと、対応するBluesky案内投稿（存在する場合）を削除しました。</p>
      <p>すでに第三者に取得・保存されたデータ、キャッシュ、アーカイブまでは削除できません。</p>
      <p>
        <a href="/manage">投稿管理画面へ戻る</a>
      </p>
    </section>
  );
}
